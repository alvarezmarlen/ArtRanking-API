version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: artranking-mongo
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: artranking
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: